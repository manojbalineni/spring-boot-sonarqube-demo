pipeline{
    agent any
    environment {
        SONARQUBE_SERVER = 'SonarQubeServer'
    }
    stages{
        stage("Checkout"){
            steps{
                git ' '
            }
        }
    }
}